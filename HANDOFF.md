# Claude Code 교사 자기주도 연수 프로그램 — 전체 인수인계 문서

> 이 문서는 Claude Desktop + Claude Code 환경에서 이어서 작업하기 위한 전체 컨텍스트입니다.  
> Claude Code에 이 문서를 통째로 붙여넣고 "여기서 이어서 작업해줘" 라고 하면 됩니다.

---

## 1. 프로젝트 개요

### 무엇을 만드는가
고등학교 교사가 **연수자(강사) 없이 혼자서** Claude Code를 배울 수 있는 자기주도 연수 프로그램.

### 핵심 설계 철학
- 게임(RPG) 방식: 던전 클리어 → 다음 던전 잠금 해제
- 평가 내재화: 매 단계마다 Claude API가 직접 프롬프트를 채점
- 모범 비교 방식: 내 프롬프트 vs 모범 프롬프트 나란히 비교 후 차이 분석
- 일지 축적: 완료 시 나만의 프롬프트 패턴 사전 자동 생성
- 디버깅 던전: 내가 만든 것의 취약점을 탐정처럼 찾아 완성도 향상

---

## 2. 확정된 설계 결정사항

### 2-1. 레벨 구조 (3단계)

| 레벨 | 이름 | LLM 경험 수준 | 진입 방식 |
|------|------|--------------|----------|
| LV.1 | 완전 초보 🌱 | 한 번도 써본 적 없음 | Claude 앱 대화창 기반 |
| LV.2 | 입문자 ⚔️ | 단순 대화·검색 정도 사용 | 앱 대화 → 터미널 전환 |
| LV.3 | 초심자 🔮 | 간단한 작업 수행 경험 있음 | 처음부터 터미널(Claude Code) |

### 2-2. 레벨 배정 방식
- 10문항 진단 설문 실시
- 총점 0~30점 → 0~8점: LV1 / 9~18점: LV2 / 19~30점: LV3
- 추천 레벨 표시 후 **직접 선택도 가능** (강제 배정 아님)

### 2-3. 던전 구조 (전 레벨 공통 5단계)

```
[던전 1] 기초 미션
[던전 2] 응용 미션
[던전 3] 심화 미션
[디버깅 던전] 탐정 모드 — 내 코드/프롬프트 취약점 찾기
[보스전] 자유 제작 — 원하는 것 1개 완성
```

### 2-4. 각 던전 내부 구조 (4막)

```
1막: 목표 산출물 공개 (결과 화면/명세 먼저 제시)
2막: 내 프롬프트 작성 → Claude API 즉시 평가 수령 (100점 만점)
3막: 모범 프롬프트 공개 → AI가 두 프롬프트 차이 분석
4막: 성장 일지 작성 (배운 것 / 어려운 점 / 내 패턴 메모)
```

### 2-5. 디버깅 던전 특이사항
- 외부 문제가 아니라 **내가 이미 작성한 프롬프트/코드**에 취약점을 Claude가 심어줌
- 취약점 3개 찾기 → 각각 수정 완료 표시 → 전부 완료 시 보너스 +20점
- 이 과정을 통해 보스전 진입 전 산출물 완성도 향상

### 2-6. 점수 체계

| 항목 | 점수 | 비고 |
|------|------|------|
| 프롬프트 AI 평가 | 0~100점 (던전당) | Claude API 자동 채점 |
| 던전 클리어 | +10점 (던전당) | 완료 버튼 클릭 시 |
| 디버깅 취약점 3개 수정 | +20점 보너스 | |
| 일지 작성 | 포트폴리오 반영 | 점수 외 자산 |

---

## 3. 가장 중요한 미해결 문제 (핵심 이슈)

### 문제: Claude Code 진입 방법 안내가 없음

현재 프로그램은 브라우저에서 실행되는 HTML 앱인데,  
실제 Claude Code는 **터미널(CLI)** 에서 동작하는 완전히 다른 환경입니다.

```
현재 HTML 프로그램 UI         실제 Claude Code 화면
──────────────────────        ──────────────────────
어두운 게임 UI                 터미널 텍스트 화면
버튼 클릭                      키보드 타이핑
브라우저 실행                   CLI 실행
```

### 해결 방향: 레벨별 작업 환경 분리

```
LV.1 완전 초보  → Claude 데스크탑 앱 대화창에서 진행
                  (터미널 없음, 설치 불필요)

LV.2 입문자    → 앱 대화창으로 시작 → 던전 3부터 터미널 도전
                  (중간에 Claude Code 설치 안내 삽입)

LV.3 초심자    → 처음부터 터미널(Claude Code) 기반
                  (설치 완료 전제 또는 프로그램 내 설치 가이드)
```

### Claude Code 설치 안내 (프로그램에 반드시 포함할 내용)

```bash
# 사전 조건: Node.js 18 이상 설치 필요
# https://nodejs.org 에서 LTS 버전 다운로드

# Claude Code 설치
npm install -g @anthropic-ai/claude-code

# 설치 확인
claude --version

# 실행 방법
claude          # 대화형 모드
claude "프롬프트"  # 단일 명령 실행
```

---

## 4. 현재 완성된 코드 (claude_training.html)

아래 파일이 현재 완성된 상태입니다.

### 구현된 기능
- [x] 10문항 설문 (진행 바 포함, 앞뒤 이동 가능)
- [x] 자동 레벨 계산 및 추천 + 수동 선택 가능
- [x] 3개 버전 카드 UI (LV1/LV2/LV3)
- [x] RPG 던전 지도 (잠금/진행중/완료 상태)
- [x] 던전 내부 3탭 구조 (내 프롬프트 / 모범 비교 / 디버깅)
- [x] Claude API 연동 프롬프트 자동 평가 (100점 만점, JSON 반환)
- [x] 프롬프트 비교 분석 (AI가 차이점 설명)
- [x] 디버깅 던전 (취약점 3개 자동 생성 → 수정 완료 체크)
- [x] 성장 일지 저장 (배운 것 / 어려운 점 / 내 패턴)
- [x] 포트폴리오 탭 (진행률 / 점수 / 패턴 사전)

### 미구현 / 보완 필요 사항
- [ ] **Claude Code 설치 및 사용 방법 안내 화면** (온보딩 섹션)
- [ ] **레벨별 작업 환경 안내** (LV1: 앱, LV2: 전환 안내, LV3: 터미널)
- [ ] LV2, LV3 미션 내용 차별화 (현재는 LV1 미션을 그대로 복제)
- [ ] OS별 설치 안내 분기 (Windows / macOS / Linux)
- [ ] 보스전 완료 후 수료증 생성 기능
- [ ] 로컬 스토리지 활용 진행 상황 저장 (브라우저 새로고침 시 초기화 방지)

---

## 5. 다음 작업 우선순위

### 최우선: 온보딩 섹션 추가

설문 시작 전에 아래 내용을 담은 온보딩 화면을 추가해야 합니다.

```
[화면 1] 환영 화면
- 이 프로그램이 뭔지 30초 설명
- Claude 앱 vs Claude Code 차이 그림으로 설명

[화면 2] 준비 확인
- "지금 Claude 데스크탑 앱을 열어두셨나요?" 체크
- LV3 선택 예정자는 추가로 "터미널이 있나요?" 체크

[화면 3] Claude Code 설치 안내 (LV3용)
- OS 선택 (Windows / macOS)
- 단계별 설치 명령어
- 설치 확인 방법

→ 확인 완료 후 설문 진행
```

### 2순위: LV2, LV3 미션 내용 실제 차별화

```
LV1 미션 키워드: AI와 첫 대화 / 학습지 요청 / 퀴즈 생성기
LV2 미션 키워드: 프롬프트 정밀 설계 / 성적 자동화 / 세특 초안 생성기
LV3 미션 키워드: 시스템 프롬프트 / Google Apps Script / 웹앱 완성도 향상
```

### 3순위: 로컬 스토리지 진행 저장

```javascript
// 저장
localStorage.setItem('trainingState', JSON.stringify(state));

// 불러오기
const saved = localStorage.getItem('trainingState');
if (saved) state = JSON.parse(saved);
```

---

## 6. Claude Code에서 이어서 작업할 때 사용할 프롬프트

### 프롬프트 A — 온보딩 섹션 추가

```
이 프로젝트는 고등학교 교사 대상 Claude Code 자기주도 연수 HTML 프로그램이야.
claude_training.html 파일을 열어줘.

현재 설문(sec-survey)이 첫 화면인데, 그 앞에 온보딩 섹션(sec-onboard)을 추가해줘.

온보딩 섹션 구성:
1. 환영 화면 — 이 프로그램 소개 + "Claude 앱 vs Claude Code" 차이를 
   간단한 다이어그램(순수 HTML/CSS)으로 시각화
2. 환경 준비 확인 — "Claude 데스크탑 앱이 열려있나요?" 체크박스
3. Claude Code 설치 안내 — OS 탭(Windows/macOS) 으로 분기,
   npm 설치 명령어를 코드 블록으로 표시, 복사 버튼 포함

기존 디자인 변수(--bg, --accent1 등)와 폰트를 그대로 유지해줘.
```

### 프롬프트 B — LV2 미션 내용 차별화

```
claude_training.html의 missions 객체에서
현재 missions.lv2와 missions.lv3가 missions.lv1을 그대로 복사하고 있어.

아래 내용으로 LV2 전용 미션 5개를 새로 작성해줘:

던전 1 — 프롬프트 정밀 설계
- 목표: 같은 결과를 역할/형식/제약 3가지를 명시해서 요청하기
- 모범 프롬프트: 성적 처리 Python 스크립트 요청 (구체적 버전)
- placeholder: 단순 요청 버전

던전 2 — 성적·출결 자동화
- 목표: 30명 학생의 국영수 점수 CSV → 평균/등수/등급 계산 후 Excel 저장하는
  Python 스크립트를 Claude Code로 생성하기
- 모범 프롬프트: 파일 경로, 등급 기준, 예외 처리 포함

던전 3 — 세특 초안 생성기
- 목표: 학생 정보(이름, 관심사, 특이사항) 입력 → 세특 초안 자동 생성 웹앱
- 모범 프롬프트: 교육부 세특 작성 가이드라인 반영 조건 포함

던전 4(디버깅) — 버그 3개 + 엣지 케이스 테스트
던전 5(보스) — 현장에서 바로 쓸 수 있는 자동화 도구
```

### 프롬프트 C — 로컬 스토리지 저장 기능

```
claude_training.html에 진행 상황 자동 저장 기능을 추가해줘.

조건:
1. state 객체가 변경될 때마다 localStorage에 자동 저장
2. 페이지 로드 시 저장된 데이터가 있으면 불러오기
3. "이어서 하기" / "처음부터 다시" 선택 모달 표시
4. 저장 키: 'claude_training_v1'
5. 저장 항목: state 전체 (설문 답변, 레벨, 진행 상태, 점수, 일지)
```

### 프롬프트 D — 수료증 생성 기능

```
claude_training.html의 보스전을 완료했을 때
포트폴리오 탭에 수료증이 생성되도록 해줘.

수료증 내용:
- 이름 입력 필드 (완료 시점에 요청)
- 완료 날짜 자동 삽입
- 선택한 레벨 / 총점 / 클리어 던전 수 표시
- "인쇄" 버튼 → window.print() 실행
- 인쇄 시 수료증만 출력되도록 @media print CSS 적용

디자인: 기존 dark theme 유지하되 인쇄용은 흰 배경으로 자동 전환
```

---

## 7. 파일 구조 (예정)

```
claude_training_project/
├── claude_training.html        ← 현재 완성된 메인 파일
├── HANDOFF.md                  ← 이 문서
├── missions/
│   ├── lv1_missions.js         ← LV1 미션 데이터 (분리 예정)
│   ├── lv2_missions.js         ← LV2 미션 데이터 (작성 필요)
│   └── lv3_missions.js         ← LV3 미션 데이터 (작성 필요)
└── assets/
    └── (필요 시 이미지 등)
```

---

## 8. Claude API 관련 메모

### 사용 모델
```javascript
model: 'claude-sonnet-4-20250514'
```

### 평가 요청 프롬프트 구조 (현재 구현된 것)
```javascript
// 100점 만점, 4개 항목 25점씩
// 응답 형식: JSON (역할명확성, 출력형식, 제약조건, 현장적용성)
// 별점(1~5), 총점, 요약, 개선점 3가지 반환
```

### API 키 처리
현재 HTML 파일에서는 API 키를 별도로 전달하지 않음.  
claude.ai 환경에서는 자동으로 처리됨.  
로컬 환경에서 실행 시 별도 API 키 입력 필드 추가 필요.

---

## 9. 이 프로젝트의 최종 목표

```
완성 후 교사가 혼자 할 수 있는 것:

1. 브라우저에서 HTML 파일 열기
2. 10문항 설문 → 레벨 자동 배정
3. Claude Code 설치 방법 온보딩에서 확인
4. 던전 1부터 순서대로 진행
5. 매 단계 AI 평가 받으며 프롬프트 실력 향상
6. 디버깅 던전에서 완성도 향상
7. 보스전에서 나만의 도구 완성
8. 수료증 + 프롬프트 패턴 사전 획득
```

---

*최종 업데이트: 2026년 4월*  
*작성: Claude (Anthropic) — claude.ai 대화에서 생성*
