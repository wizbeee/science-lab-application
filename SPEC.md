# 바이브 코딩 자기주도 학습 프로그램 — 설계 문서 (SPEC)

> 최종 업데이트: 2026-04-10
> 상태: **검토 대기 중** — 확정 후 구현 진행

---

## 1. 대전제 (절대 흔들리지 않는 전제)

> **사용자 = 코딩을 한 번도 다뤄본 적 없는 완전 초보자 (고등학교 교사)**

이 전제에서 파생되는 불변 원칙:

- 터미널, CLI, Node.js, npm 등 **기술 용어를 그대로 노출하지 않는다**
- 타이핑 요구 최소화 → **복사 버튼** 필수
- "이미 알고 있겠지"라는 가정 금지 → **모든 것은 0부터 설명**
- 실패에 대한 심리적 안전 → "틀린 프롬프트는 없습니다" 톤 유지
- OS 분기 자동 처리 (Mac / Windows)

---

## 2. 목표와 비목표

### ✅ 목표
1. **바이브 코딩 체험** — 자연어만으로 뭔가 작동하는 것을 만들어본다
2. **프롬프트 작성 실력 강화** — 같은 요청도 어떻게 쓰느냐에 따라 결과가 달라짐을 체감
3. **의도와 다른 결과 대처 능력** — 엉뚱한 결과가 나왔을 때 되돌리고 수정하는 법
4. **초보 → 중급자 수준** 도달 (전문가는 비목표)

### ❌ 비목표
- 코드 문법(Python, JavaScript) 자체를 가르치는 것
- 전문 개발자 육성
- Git, 배포, 테스트 등 개발 프로세스 전반
- 레벨 분기를 통한 상급 과정

---

## 3. 전체 화면 흐름

```
┌────────────────────────────────────────────────────────┐
│  [0] 환영 화면                                          │
│      - "바이브 코딩이 뭔가요?" 30초 설명                 │
│      - "시작하기" 버튼                                   │
└────────────────┬───────────────────────────────────────┘
                 ↓
┌────────────────────────────────────────────────────────┐
│  [1] 설치 가이드 (Claude Desktop App)                   │
│      - OS 자동 감지 (Mac / Windows)                     │
│      - 스크린샷 + 복사 가능한 링크                       │
│      - "설치 완료했어요" 체크박스 → 다음                 │
└────────────────┬───────────────────────────────────────┘
                 ↓
┌────────────────────────────────────────────────────────┐
│  [2] 5문항 설문                                         │
│      - 실력 위치 (2문항) + 관심사 (3문항)                │
│      - 결과: 안내 강도(친절/표준) + 미션 예시 맞춤       │
└────────────────┬───────────────────────────────────────┘
                 ↓
┌────────────────────────────────────────────────────────┐
│  [3] 미션 허브 (홈)                                     │
│      - 3개 미션 카드 (순차 잠금)                         │
│      - 좌측: Claude Desktop 스타일 사이드바              │
│      - 진행률 표시                                      │
└────────────────┬───────────────────────────────────────┘
                 ↓
┌────────────────────────────────────────────────────────┐
│  [4] 미션 진행 (3막 구조)                               │
│      1막: 목표 공개                                      │
│      2막: 내 프롬프트 작성 → AI 채점                    │
│      3막: 모범 프롬프트 비교 + "의도 어긋남 대처" 실습   │
└────────────────┬───────────────────────────────────────┘
                 ↓
┌────────────────────────────────────────────────────────┐
│  [5] 완료 화면                                          │
│      - 내가 쓴 프롬프트 3개 모아보기 (나의 성장 기록)     │
│      - 수료증 (선택) + 다음 단계 안내                    │
└────────────────────────────────────────────────────────┘
```

---

## 4. 5문항 설문 (확정안)

### 실력/레벨 위치 (2문항)

**Q1. AI 챗봇(ChatGPT, Claude 등)을 사용해본 적이 있나요?**
- ① 전혀 없음 (0점)
- ② 1~2번 써봤음 (1점)
- ③ 가끔 씀 (2점)
- ④ 자주 씀 (3점)

**Q2. "파일을 다운로드해서 설치한다"는 말을 들었을 때 어떠세요?**
- ① 막막함 (0점)
- ② 해본 적 있는 것 같음 (1점)
- ③ 혼자 할 수 있음 (2점)

### 관심사 맞춤 (3문항)

**Q3. 바이브 코딩으로 가장 먼저 만들어보고 싶은 건?**
- ① 업무 자동화 (성적/출결/문서)
- ② 수업 자료 (학습지/퀴즈/PPT)
- ③ 일상 도구 (가계부/일정/메모)
- ④ 재미있는 것 (게임/애니메이션)
- ⑤ 아직 모르겠음

**Q4. 결과물이 어떤 형태면 좋겠어요?**
- ① 웹페이지 (브라우저에서 열림)
- ② 엑셀/파일 결과물
- ③ 간단한 앱 화면
- ④ 상관없음

**Q5. 학습 스타일은?**
- ① 천천히 꼼꼼하게
- ② 빠르게 일단 해보기
- ③ 예시 보면서 따라하기

### 결과 분류 로직

```
[안내 강도] = Q1 + Q2 합산
  0~1점 → 친절모드 (모든 단계 설명 + 스크린샷)
  2~5점 → 표준모드 (핵심만)

[미션 예시] = Q3(관심사)로 소재 결정
  → 관심사 5개 × 미션 소재 세트 5종 (상세 내용은 §5.5 참조)

[결과 형태 힌트] = Q4는 미션 1의 모범 프롬프트에 "○○ 형태로 만들어줘" 문구 삽입에 사용

[튜토리얼 표시] = Q5
  ① → 긴 설명 + 순차 진행
  ② → 버튼만, 바로 실행
  ③ → 예시 코드 먼저 표시
```

---

## 5. 미션 구조 (4개) ✅ 확정

### 학습 설계 원칙
완전 초보자용 학습 곡선: **성공 경험 → 기술 습득 → 회복탄력성 → 자율 적용**

작은 성공의 누적이 완전 초보자에겐 가장 중요. 4개는 "미션이 여러 개라는 체감"과 "피로도 누적" 사이 균형점.

### 미션 1 — 첫 대화 (성공 경험)
- **목표**: Claude에게 가장 단순한 걸 요청해서 **뭐라도 결과가 나오는 경험**
- **핵심 학습**: "AI에게 말을 걸면 진짜 뭐가 나온다"는 확신
- **프롬프트 예시** (설문 Q3·Q4로 자동 치환):
  - 업무×파일: "학생 성적 평균 계산하는 파이썬 코드 만들어줘"
  - 수업×웹: "수학 퀴즈 10문제 웹페이지 만들어줘"
  - 일상×앱: "가계부 기록하는 간단한 앱 만들어줘"

### 미션 2 — 조건 붙여 요청하기 (기술 습득)
- **목표**: 같은 요청이라도 **역할 / 형식 / 제약** 3가지를 명시해서 정밀 요청
- **핵심 학습**: 막연한 요청 vs 구체적 요청의 결과 차이 체감
- **특징**: 사용자가 이전 미션에서 쓴 프롬프트를 자동으로 가져와서 "이걸 어떻게 업그레이드할까?" 형태로 연속성 제공

### 미션 3 — 엉뚱한 결과 대처하기 (회복탄력성) ⭐ 독립 미션
- **왜 독립 미션인가**: 이게 바이브 코딩의 진짜 핵심. 프롬프트 잘 쓰는 건 20%, 엉뚱한 결과 회복이 80%. 미션 2에 묻으면 1회성 경험으로 끝남. 독립시키면 여러 시나리오 연습 가능.
- **목표**: 의도와 다른 결과가 나왔을 때 3가지 회복 기술 습득
- **4개 시나리오** (단계적 진행):
  1. **완전히 다른 결과** — 웹앱을 원했는데 스크립트가 나옴 → "구체화" 기술
  2. **부분만 잘못됨** — 대체로 맞는데 한 부분이 이상 → "부분 수정" 기술
  3. **점점 이상해짐** — 수정할수록 망가짐 → "되돌리기" 기술
  4. **이전 버전이 더 나았음** — 판단력 기르기 → "버전 관리" 기술
- **진행 방식**: 각 시나리오마다 엉뚱한 결과 화면 → 내가 직접 재요청 작성 → AI 평가

### 미션 4 — 자유 제작 (자율 적용)
- **목표**: 앞의 3개 미션을 통합해서 **진짜 내가 쓸 도구** 만들기
- **핵심 학습**: 혼자서도 처음부터 끝까지 갈 수 있다는 자신감
- **지원**: 막히면 힌트 버튼 (설문 관심사 기반 제안)

### 각 미션 내부 구조 (3막)

```
[1막] 목표 공개
  - 결과물 미리보기 (스크린샷 또는 간단한 설명)
  - "어떻게 요청하면 이게 나올까요?" 질문

[2막] 내 프롬프트 작성 → AI 채점
  - 텍스트박스에 내가 직접 작성
  - "채점받기" 버튼 클릭 (API 키 없으면 "모범 프롬프트 보기"로 전환)
  - 100점 만점 + 4개 항목별 별점 (초보자용 쉬운 말로)
    · 뭘 원하는지 (25점) — 원하는 결과물이 명확한가
    · 얼마나 자세한지 (25점) — 필요한 정보를 빠짐없이 담았는가
    · 상황 설명 (25점) — 왜 필요한지, 누가 쓸 건지 맥락이 있는가
    · 실제로 쓸 수 있을지 (25점) — 현실적으로 동작 가능한 요청인가
  - 개선점 3가지 자동 피드백 (친근한 말투)

[3막] 모범 프롬프트 비교
  - 모범 프롬프트 공개
  - AI가 "내 것 vs 모범" 차이 분석 (자동)
  - 내가 쓴 프롬프트 자동 저장 (성장 기록용)
```

### 미션별 난이도 / 소요 시간 (예상)

| 미션 | 소요 | 난이도 | 심리적 특징 |
|------|------|--------|-------------|
| 1. 첫 대화 | 10분 | ★☆☆ | "어라? 진짜 되네" |
| 2. 조건 붙이기 | 15분 | ★★☆ | "이렇게 쓰면 더 좋구나" |
| 3. 엉뚱한 결과 대처 | 20분 | ★★★ | "문제가 생겨도 당황 안 해" |
| 4. 자유 제작 | 30분 | ★★★ | "이제 혼자 할 수 있다" |

**총 약 75분** — 1~2회 세션으로 완주 가능한 분량

---

## 5.5 미션 소재 — 관심사별 상세 시나리오

미션 1, 2는 설문 Q3(관심사)에 따라 소재가 결정됨. 미션 3은 엉뚱한 결과 대처 자체가 학습 목표이므로 **모든 관심사 공통 시나리오** 사용. 미션 4는 사용자 자율.

### [관심사 A] 업무 자동화 (성적/출결/문서)

**미션 1 — 첫 대화**
- 목표: 30명 학생의 성적 평균을 계산하는 간단한 도구
- 초보 프롬프트 예시: *"성적 계산하는 거 만들어줘"*
- 모범 프롬프트:
  > *"30명 학생의 국어/영어/수학 점수가 엑셀로 있어. 각 학생의 평균 점수와 반 전체 평균을 계산해서 보여주는 파이썬 코드를 만들어줘. 결과는 점수 순으로 정렬해줘."*

**미션 2 — 조건 붙이기**
- 목표: 같은 성적 계산기에 "출결 반영 + 등급 자동 분류" 추가
- 모범 프롬프트:
  > *"[역할] 너는 고등학교 교무 업무를 돕는 파이썬 전문가야. [과제] 앞서 만든 성적 계산 코드에 출결 차감(결석 -2점, 지각 -0.5점)과 등급 자동 분류(90↑ A, 80↑ B, 70↑ C, 그 외 D)를 추가해줘. [형식] 주석을 한글로 달아주고, 파일 경로는 내가 바꿀 수 있게 맨 위에 변수로 빼줘. [제약] 엑셀 파일 형식은 건드리지 마."*

### [관심사 B] 수업 자료 (학습지/퀴즈/PPT)

**미션 1 — 첫 대화**
- 목표: 수학 퀴즈 웹페이지 하나 만들기
- 초보 프롬프트 예시: *"수학 문제 만들어줘"*
- 모범 프롬프트:
  > *"중학교 2학년 일차방정식 퀴즈 10문제를 HTML 웹페이지로 만들어줘. 학생이 답을 입력하면 정답인지 바로 알려주고, 마지막에 총점을 보여주는 기능도 넣어줘."*

**미션 2 — 조건 붙이기**
- 목표: 퀴즈에 난이도 조정 + 오답 해설 추가
- 모범 프롬프트:
  > *"[역할] 너는 중학교 수학 교사이자 교육용 웹페이지 디자이너야. [과제] 앞서 만든 일차방정식 퀴즈에 난이도 선택(상/중/하)과 오답일 때 풀이 과정을 단계별로 보여주는 기능을 추가해줘. [형식] 디자인은 학생이 집중할 수 있게 밝은 색으로, 문제는 큰 글씨로 해줘. [제약] 외부 라이브러리 없이 순수 HTML/CSS/JS로만 만들어줘."*

### [관심사 C] 일상 도구 (가계부/일정/메모)

**미션 1 — 첫 대화**
- 목표: 간단한 할 일 목록 웹앱
- 초보 프롬프트 예시: *"할 일 목록 만들어줘"*
- 모범 프롬프트:
  > *"할 일을 입력하면 목록에 추가되고, 체크박스로 완료 표시를 할 수 있는 웹페이지를 만들어줘. 완료된 항목은 취소선이 그어지게 해줘."*

**미션 2 — 조건 붙이기**
- 목표: 할 일 앱에 저장 기능 + 우선순위 추가
- 모범 프롬프트:
  > *"[역할] 너는 개인 생산성 앱을 만드는 웹 개발자야. [과제] 앞서 만든 할 일 목록에 브라우저를 새로고침해도 저장되는 기능(localStorage)과 우선순위(높음/보통/낮음) 색깔 표시를 추가해줘. [형식] 모바일에서도 잘 보이게 반응형으로 만들어줘. [제약] 외부 라이브러리 없이."*

### [관심사 D] 재미있는 것 (게임/애니메이션)

**미션 1 — 첫 대화**
- 목표: 숫자 맞추기 게임
- 초보 프롬프트 예시: *"게임 만들어줘"*
- 모범 프롬프트:
  > *"1부터 100 사이의 숫자를 컴퓨터가 정하면, 내가 숫자를 입력해서 맞추는 웹페이지 게임을 만들어줘. 내 숫자가 더 크면 '작게', 작으면 '크게'라고 알려주고, 몇 번 만에 맞췄는지 세어줘."*

**미션 2 — 조건 붙이기**
- 목표: 게임에 난이도/시간 제한/효과음 추가
- 모범 프롬프트:
  > *"[역할] 너는 재미있는 웹 게임을 만드는 게임 디자이너야. [과제] 앞서 만든 숫자 맞추기 게임에 난이도 3단계(쉬움 1~50, 보통 1~100, 어려움 1~500), 30초 시간 제한, 맞췄을 때 축하 애니메이션을 추가해줘. [형식] 버튼과 글씨는 크고 선명하게. [제약] 외부 이미지나 사운드 파일은 사용하지 말고 이모지와 CSS 애니메이션으로만 해줘."*

### [관심사 E] 아직 모르겠음 (기본 — 학습지 생성기)

**미션 1 — 첫 대화**
- 목표: 영어 단어 암기 카드 웹페이지
- 초보 프롬프트 예시: *"영어 단어 공부하는 거 만들어줘"*
- 모범 프롬프트:
  > *"영어 단어 10개와 뜻이 들어있는 암기 카드 웹페이지를 만들어줘. 카드를 클릭하면 앞면(영단어)과 뒷면(뜻)이 뒤집히게 해줘."*

**미션 2 — 조건 붙이기**
- 모범 프롬프트:
  > *"[역할] 너는 학습 웹사이트를 만드는 교육 프로그래머야. [과제] 앞서 만든 영어 단어 카드에 내가 직접 단어를 추가할 수 있는 입력 폼과, 오늘 외운 단어 개수를 기록하는 기능을 추가해줘. [형식] 카드 뒤집기는 부드러운 애니메이션으로. [제약] 브라우저를 껐다 켜도 내가 추가한 단어가 남아있게 해줘."*

### 미션 3 — 엉뚱한 결과 대처 (공통 시나리오 4개)

관심사와 무관하게 모든 사용자가 동일한 시나리오 경험:

**시나리오 1 — 완전히 다른 결과**
- 상황: "학생 관리 프로그램 만들어줘" 라고만 함
- 나온 결과: 커맨드라인에서 돌아가는 단순 스크립트 (내가 원한 건 웹앱이었음)
- 학습 기술: **구체화** — "학생 이름·번호·점수를 입력하고 저장하는 웹페이지로 만들어줘. 브라우저에서 열 수 있게."

**시나리오 2 — 부분만 잘못됨**
- 상황: 대부분 맞는데 저장 기능이 안 됨 (새로고침하면 사라짐)
- 학습 기술: **부분 수정** — "방금 만든 코드에서 저장 부분만 localStorage로 바꿔줘. 다른 부분은 건드리지 말고."

**시나리오 3 — 점점 이상해짐**
- 상황: 수정을 3번 했는데 갈수록 이상해짐 (원래 작동하던 것도 깨짐)
- 학습 기술: **되돌리기** — "아까 2번째 버전으로 돌아가자. 거기서부터 다시 시작할게."

**시나리오 4 — 이전 버전이 더 나았음**
- 상황: 새 기능 추가했는데 원래 기능이 불편해짐
- 학습 기술: **버전 관리** — "이전 버전의 깔끔했던 디자인은 유지하고, 새 기능만 추가하는 식으로 다시 만들어줘."

### 성장 일지 (최소화 확정)

```
❌ 제거: "알게 된 점", "배운 점", "어려운 점"
✅ 유지: 내가 쓴 프롬프트 (원본 그대로 저장)

결과: 완료 시 "내 프롬프트 3개" 자동 모음
      = 나만의 성장 기록
```

---

## 6. 설치 가이드 설계

### Claude Desktop 앱 설치 (LV 분기 없음 — 모두 필수)

```
[Mac 분기]
1. 스크린샷: 공식 사이트
2. https://claude.ai/download 링크 (복사 버튼)
3. "Mac용 다운로드" 클릭 안내
4. .dmg 파일 → 드래그 설치 애니메이션
5. 로그인 화면까지 도달 확인

[Windows 분기]
1. 스크린샷: 공식 사이트
2. https://claude.ai/download 링크 (복사 버튼)
3. "Windows용 다운로드" 클릭 안내
4. .exe 파일 → 설치 마법사 따라가기
5. 로그인 화면까지 도달 확인
```

**OS 자동 감지**:
```javascript
const isMac = navigator.platform.toLowerCase().includes('mac');
const isWin = navigator.platform.toLowerCase().includes('win');
```

**체크포인트**:
- "Claude 데스크탑 앱에 로그인했어요" ✅ 체크 후 다음 진행

---

## 7. "의도와 다른 결과 대처" 학습 모듈 (핵심 차별화)

### 배치 위치
미션 2의 3막 중간에 삽입

### 학습 시나리오

```
단계 1: 일부러 애매한 프롬프트 제공
  예: "학생 관리 프로그램 만들어줘"

단계 2: Claude가 예상과 다른 결과 생성 (시뮬레이션)
  예: 단순 텍스트 출력 스크립트가 나옴
      (사용자는 웹앱을 원했을 것)

단계 3: "어? 내가 원한 건 이게 아닌데" 자각 유도
  - "이게 원하던 건가요?" 질문
  - 대부분 "아니오" 선택

단계 4: 3가지 재요청 기술 학습
  ① 구체화 기술
     ❌ "더 자세히 말해줘"
     ✅ "입력 화면과 저장 기능이 있는 웹페이지로 만들어줘"

  ② 부분 수정 기술
     ❌ 전체 다시 요청
     ✅ "방금 만든 코드에서 저장 부분만 로컬스토리지로 바꿔줘"

  ③ 되돌리기 기술
     ❌ 계속 수정 시도
     ✅ "아까 2번째 버전으로 돌아가서 시작하자"

단계 5: 직접 재요청 연습
  - 위 3가지 중 하나 골라서 실제 작성
  - AI 평가 받기
```

---

## 8. Claude Desktop UI 모방 디자인 가이드

### 참고 소스
**실제 claude.ai 라이브 사이트에서 CSS 변수 직접 추출** (2026-04-10 기준)
- 테마명: `[data-theme="claude"]`
- 모드: `data-mode="light"` / `data-mode="dark"`
- 실제 사용하는 디자인 토큰을 그대로 사용 → 완전 동일 룩앤필

### 레이아웃
```
┌─────────────────────────────────────────────────────┐
│ [사이드바]     │ [메인 콘텐츠]                      │
│                │                                    │
│ + 새 대화      │  ┌─ 메시지 영역 ─────────────┐    │
│                │  │                            │    │
│ 미션 1 ✓      │  │  Claude: 안녕하세요...     │    │
│ 미션 2 ●      │  │                            │    │
│ 미션 3 🔒      │  │  나: 프롬프트 작성...      │    │
│                │  │                            │    │
│ ─────          │  └────────────────────────────┘    │
│                │                                    │
│ 설정           │  ┌─ 입력창 ──────────────────┐    │
│ 도움말         │  │ 여기에 입력하세요...       │    │
│                │  └────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

### 색상 팔레트 — 실제 Claude 토큰 (검증됨 ✅)

#### 🌙 Dark Mode (`[data-theme="claude"][data-mode="dark"]`)
```css
/* 배경 — 어두울수록 숫자 증가 */
--bg-000: #2c2c2a;   /* 카드/입력창 배경 (가장 밝은 표면) */
--bg-100: #1f1f1e;   /* 메인 배경 (body) */
--bg-200: #171716;   /* 사이드바/더 어두운 층 */
--bg-300: #121212;   /* 가장 어두운 층 */
--bg-400: #0a0a0a;   /* 거의 검정 */

/* 텍스트 — 밝을수록 낮은 숫자 */
--text-000: #f8f8f6;  /* 메인 텍스트 (크림 화이트) */
--text-100: #f8f8f6;  /* 동일 */
--text-200: #c3c2b7;  /* 보조 텍스트 */
--text-300: #c3c2b7;  /* 동일 */
--text-400: #97958c;  /* 희미한 텍스트 */

/* 포인트 */
--accent-brand: #d97757;  /* Claude 오렌지 (변하지 않음) */
--brand-000: #c6613f;     /* 짙은 오렌지 (호버/눌림) */

/* 상태 */
--border: #e2e1da;        /* (실사용 시 낮은 opacity로) */
--success: #639900;
--warning: #b87700;
--danger: #e34a4a;

/* 메시지 버블 */
--user-bubble-bg: #2c2c2a;     /* 사용자 메시지 = bg-000 */
--claude-bubble-bg: transparent; /* Claude 메시지 = 배경 없음 */
```

#### ☀️ Light Mode (`[data-theme="claude"][data-mode="light"]`)
```css
/* 배경 — 어두울수록 숫자 증가 */
--bg-000: #ffffff;   /* 순백 (최상위 표면) */
--bg-100: #f8f8f6;   /* 메인 배경 (body, 크림 화이트) */
--bg-200: #f4f4f1;   /* 카드 */
--bg-300: #efeeeb;   /* 호버 */
--bg-400: #e6e5e0;   /* 경계/구분선 */

/* 텍스트 */
--text-000: #121212;  /* 메인 텍스트 (거의 검정) */
--text-100: #121212;  /* 동일 */
--text-200: #373734;  /* 보조 텍스트 */
--text-300: #373734;  /* 동일 */
--text-400: #7b7974;  /* 희미한 텍스트 */

/* 포인트 — 다크모드와 동일한 오렌지 */
--accent-brand: #d97757;  /* Claude 오렌지 */
--brand-000: #c6613f;     /* 짙은 오렌지 */

/* 상태 */
--border: #1f1f1e;        /* (실사용 시 낮은 opacity로) */
--accent-link: #2977d6;   /* 파란 링크 */
--success: #578a00;
--warning: #a86b00;
--danger: #cf3a3a;

/* 메시지 버블 */
--user-bubble-bg: #f4f4f1;     /* 사용자 메시지 = bg-200 */
--claude-bubble-bg: transparent;
```

### 핵심 특징
- **Claude 오렌지 `#d97757`** 는 라이트/다크 모드 모두 동일 → 브랜드 일관성
- **크림톤 화이트 `#f8f8f6`** 이 특징적 (완전 흰색 아님)
- **채도가 낮은 회갈색 계열** — 차가운 회색이 아닌 따뜻한 회색
- **사이드바는 투명 배경** (메인 배경 위에 살짝 어두운 톤으로)

### 모드 전환 구현
```javascript
// 초기값: OS 설정 따름 (사용자가 바꾼 적 없으면)
const saved = localStorage.getItem('theme-mode');
const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
const initialMode = saved || (prefersDark ? 'dark' : 'light');

document.documentElement.setAttribute('data-theme', 'claude');
document.documentElement.setAttribute('data-mode', initialMode);

// OS 설정 변경을 실시간 감지 (사용자가 수동 변경 안 한 경우에만)
window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', (e) => {
  if (!localStorage.getItem('theme-mode')) {
    document.documentElement.setAttribute('data-mode', e.matches ? 'dark' : 'light');
  }
});

// 사용자 수동 토글 시
function toggleTheme() {
  const current = document.documentElement.getAttribute('data-mode');
  const next = current === 'dark' ? 'light' : 'dark';
  document.documentElement.setAttribute('data-mode', next);
  localStorage.setItem('theme-mode', next);
}
```

### 폰트
```css
font-family:
  "Anthropic Sans",  /* 실제 Claude 폰트 — 비공개, fallback 됨 */
  system-ui,
  "Segoe UI",        /* Windows */
  -apple-system,     /* macOS */
  "Malgun Gothic",   /* 한글 Windows */
  "Apple SD Gothic Neo", /* 한글 macOS */
  sans-serif;
```

### 주요 컴포넌트
1. **사이드바** — 좌측 고정, 접기/펼치기 가능
2. **메시지 버블** — 사용자(회색 배경) / Claude(배경 없음)
3. **입력창** — 하단 고정, 둥근 모서리, 전송 버튼(오렌지)
4. **카드** — 둥근 모서리 12px, 은은한 그림자
5. **버튼** — 주 버튼은 오렌지, 보조는 테두리만

### 반응형
- 데스크탑: 사이드바 + 메인 (2단)
- 모바일: 햄버거 메뉴 + 메인 (1단)

---

## 9. 기술 스택

- **단일 HTML 파일** (기존과 동일)
- 순수 HTML / CSS / JavaScript (프레임워크 없음)
- Claude API 연동: `window.claude.complete()` (claude.ai 환경)
- 진행 상황 저장: `localStorage`
- 외부 의존성: 없음 (완전 오프라인 가능, API 호출 제외)

---

## 10. 파일 구조

```
claude_training_project/
├── claude_training.html    ← 메인 파일 (전면 재작성)
├── SPEC.md                 ← 이 문서
└── HANDOFF.md              ← 기존 인수인계 (참고용 보관)
```

---

## 11. 구현 순서

1. **HTML 골격** — 5개 주요 화면 컨테이너 + 라우팅
2. **CSS** — Claude Desktop 스타일 팔레트/레이아웃/컴포넌트
3. **환영 화면 + 설치 가이드** — OS 자동 감지 포함
4. **5문항 설문** — 로직 포함
5. **미션 허브** — 3개 카드 + 잠금 상태
6. **미션 상세 (3막)** — 1막 목표 → 2막 채점 → 3막 비교
7. **미션 2의 "의도 어긋남 대처" 모듈**
8. **완료 화면 + 프롬프트 모음**
9. **localStorage 저장/복원**
10. **크로스 브라우저 / Mac·Windows 테스트 가이드**

---

## 12. 수료증 기능 (재미 요소) ✅ 포함 확정

### 설계 원칙
- **부담 없이, 재미로** — 평가 도구 아닌 완주 기념품
- **심플한 디자인** — Claude 오렌지 포인트 + 크림 화이트 배경
- **인쇄 가능** — `window.print()` + `@media print` CSS

### 수료증 내용
```
╔══════════════════════════════════════════╗
║                                          ║
║         ✦ 바이브 코딩 수료증              ║
║                                          ║
║   [이름 입력]  님                         ║
║                                          ║
║   위 사람은 Claude와 함께                 ║
║   바이브 코딩 4개 미션을 완료하였기에      ║
║   이를 증명합니다                         ║
║                                          ║
║   완료 일자: 2026-04-10                   ║
║   작성한 프롬프트: 4개                    ║
║                                          ║
║              ✦ Claude ✦                  ║
║                                          ║
╚══════════════════════════════════════════╝
         [인쇄]   [이미지로 저장]
```

### 배치
- 미션 4(자유 제작) 완료 직후 자동 표시
- 완료 화면에서 언제든 다시 볼 수 있음

### 기술 구현
```css
@media print {
  body * { visibility: hidden; }
  .certificate, .certificate * { visibility: visible; }
  .certificate { position: absolute; left: 0; top: 0; }
  /* 인쇄 시 다크모드 → 라이트모드 강제 */
  .certificate { background: #ffffff !important; color: #121212 !important; }
}
```

---

## 13. 확정된 결정사항 요약 ✅

- [x] 미션 개수: **4개** (성공 → 기술 → 회복 → 자율)
- [x] "의도 어긋남 대처": **독립 미션 3번**으로 분리
- [x] 성장 일지: 내가 쓴 프롬프트만 저장 (알게 된 점/배운 점 제거)
- [x] 설문: **5문항** (실력 2 + 관심사 3)
- [x] 색상 팔레트: **실제 claude.ai 라이브 토큰** 사용 (라이트/다크 모두)
- [x] 수료증: **포함** (재미 요소)
- [x] OS: Mac / Windows 자동 분기
- [x] 설치: Claude Desktop App (Node.js/터미널 X)
- [x] 다크/라이트 모드: **둘 다 지원**, **초기값은 OS 설정 따름** (`prefers-color-scheme`)
- [x] 배포: **GitHub Pages URL 호스팅** (관리자 1회 설정 → 교사는 URL 클릭)
- [x] API 키: **옵션 입력**, 없으면 자가 평가 모드로 진행 가능 + 발급 가이드 포함
- [x] Claude 앱 복붙 워크플로우: **포함** (API 채점과 병행)
- [x] 미션 1, 2 소재: **관심사 5종 × 5개 시나리오** (§5.5 상세)
- [x] 미션 3 시나리오: **공통 4개** (완전다름/부분잘못/점점이상/이전이더나음)
- [x] 채점 항목 이름: **쉬운 말로 변경** (뭘 원하는지/얼마나 자세한지/상황 설명/실제로 쓸 수 있을지)

---

## 14. 구현 준비 완료 🚀

모든 설계 결정이 확정되었습니다. 다음 단계:

1. `claude_training.html` 전면 재작성 시작
2. 완성 후 로컬 브라우저에서 테스트
3. GitHub Pages 배포 가이드 제공 (스크린샷 포함)
4. 실제 URL 공유 → 교사들 연수 시작

---

*SPEC 최종 확정일: 2026-04-10*
